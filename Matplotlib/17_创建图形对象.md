# 创建图形对象：手动创建 Figure 对象

## plt.figure()

```python
fig = plt.figure(figsize=(8, 5))
plt.plot([1, 2, 3], [1, 4, 9])
plt.show()
```

## 常用参数

figsize (宽,高)  dpi 分辨率  facecolor 背景色

## Figure 方法

fig.add_subplot 添加子图  fig.add_axes 按坐标添加区域  fig.savefig 保存  fig.suptitle 总标题
