# 保存图片：导出 PNG/JPG/SVG 等

## plt.savefig(fname)

需在 show 之前调用。

```python
plt.plot([1, 2, 3], [1, 4, 9])
plt.savefig('out.png', dpi=300, bbox_inches='tight')
plt.show()
```

格式 png jpg svg pdf。参数 dpi bbox_inches format
