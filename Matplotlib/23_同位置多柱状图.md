# 同位置多柱状图：分组柱状图

用 x 偏移实现多组并排。

```python
x = np.arange(4)
width = 0.25
plt.bar(x - width, y1, width, label='A')
plt.bar(x, y2, width, label='B')
plt.bar(x + width, y3, width, label='C')
plt.xticks(x, ['Q1', 'Q2', 'Q3', 'Q4'])
plt.legend()
```
