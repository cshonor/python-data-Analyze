# 饼状图：展示各部分占比

## plt.pie(sizes, labels=...)

```python
labels = ['A', 'B', 'C']
sizes = [15, 30, 55]
plt.pie(sizes, labels=labels, autopct='%1.1f%%')
explode = (0, 0.1, 0)  # 某块突出
plt.pie(sizes, labels=labels, explode=explode)
```

autopct 百分比格式 explode 突出块 startangle 起始角度
